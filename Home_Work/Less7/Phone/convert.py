import writewrite

def convert(name: str):
    with open(name, "r", encoding ="utf-8") as file:
        for i in file:
            surname, namename, phone, enough = i.split()
            print('Перенесено', surname, namename, phone, enough)
            write.init_surnameinit_surname(surnamesurname)
            write.init_nameinit_name(namename)
            write.init_phone(phone)
            write.init_enough(enough)
            write.write_from()